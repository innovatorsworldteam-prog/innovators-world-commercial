# SEO foundation

Worker-native SEO helpers for the canonical `innovatorsworld.org` domain.

The current layer provides reusable metadata, organization/FAQ JSON-LD, robots.txt and sitemap generation, and legacy `.in` host redirect handling. Integration into the Worker request handler remains a separate step so existing routing is not altered blindly.
